import 'package:flutter/material.dart';

class FootballTeamDetailScreen extends StatelessWidget {
  const FootballTeamDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("team"),
    );
  }
}
