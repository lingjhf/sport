import 'package:flutter/material.dart';

class FootballMatchDetailScreen extends StatelessWidget {
  const FootballMatchDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FootballMatchDetailScreen"),
    );
  }
}
